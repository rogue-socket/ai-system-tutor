# Loop 1 — Notes

*Fill these in after your code passes WIN.md, while the experience is fresh. Three short paragraphs each. Your own words — copy-paste from BREAK.md or the tutor defeats the point.*

## Concept

What this loop teaches in your own words. What an "agent" actually is at the bottom layer, why a single LLM call isn't one, what the loop adds.

## The break

What specifically failed when you first ran `agent.py`. The actual error or wrong behavior. What you thought was wrong vs what was actually wrong. Include the failure that took you longest to figure out.

## The fix

What changes resolved it, and *why* — the underlying reason, not just the diff. If you tried a fix that didn't work, mention it; that's information.
